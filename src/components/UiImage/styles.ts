import { SxProps, Theme } from '@mui/material';

const styles: { wrapper: SxProps<Theme> } = {
  wrapper: {
    img: {
      width: '100%',
      height: '100%',
      objectFit: 'cover',
    },
  },
};

export default styles;
