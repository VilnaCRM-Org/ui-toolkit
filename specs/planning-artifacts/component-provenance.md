# Component Provenance Registry

Central source-provenance registry for the toolkit, mandated by
`architecture.md#provenance--governance-patterns` and `epics.md` (FR: provenance
tracked centrally with `crm | website | new` source labels and rationale).

For each delivered or enhanced component, record:

- **source** — `crm` (canonical behavior), `website` (visual/variant gap-fill), or `new`
- **rationale** — why that source is canonical for this component
- **alignment notes** — behavior/visual decisions and any documented deviations

New entries are appended by the delivering story. Existing entries are updated
(not rewritten) when a later story enhances a component.

## Registry

### Epic 1 — Core Controls

| Component                                   | Source | Rationale                                                                                         | Alignment notes / deviations                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `UiButton` (`src/components/ui-button`)     | `crm`  | Core control already present from the canonical toolkit; behavior follows `crm` button semantics. | States `rest/hover/active/disabled` styled from `crm`-canonical tokens (`ui-color-theme`). **Note:** Board A scopes the button to these states (`prd.md:78`); `error`/`invalid` is optional per FR-04 (`prd.md:179`), only in form-validation contexts — outside this scope and not implemented (deferred, not a parity gap). `socialButton` variant per-state visual coverage deferred (non-blocking) — see Story 1.2 artifact. **Story 1.3:** keyboard operability (Enter/Space) and disabled tab-order exclusion validated; the persistent focus-ring visual is deferred to the accessibility-visuals PR (F2).                                                                                                                                                                                                                                                                                             |
| `UiInput` (`src/components/ui-input`)       | `crm`  | Canonical text-field behavior; wraps MUI `TextField` with the shared contract.                    | States `rest/hover/active/disabled/error` styled from `crm` tokens. **Deviation:** `active` ≡ `Mui-focused` — an MUI text field has no distinct pressed appearance separate from focus; the board "active" cell maps to the focused appearance (`ui-input/theme.ts:15-17`). **Story 1.3:** added dev-only guidance (`console.warn`) when the field has no accessible name or is in `error` with no `helperText` (non-breaking, stripped in production); focus-width visual deferred (F7).                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `UiCheckbox` (`src/components/ui-checkbox`) | `crm`  | Canonical checkbox behavior with a custom `.ui-checkbox-box` affordance.                          | States `rest/hover/checked/disabled/error` styled from `crm` tokens; `error` also sets `aria-invalid`. `active` (pressed) is N/A in the state grid. **Story 1.3:** added optional `required` (native input attribute for assistive tech) and `helperText` (linked via `aria-describedby`); the visible keyboard focus indicator remains deferred to the accessibility-visuals PR (F1, Critical). **Story 5.1 ruling:** Board A's titled "option group" cluster (heading `448:25628`, frame `448:25615`, rows `448:25616`/`448:25619`/`448:25622`/`448:25625`) is board-painted but absent from `prd.md` §4.1, and is ruled a **consumer composition** of `UiCheckbox` rows under the Story 3.5 composition-boundary precedent — no separate checkbox-group component is owed.                                                                                                                                 |
| `UiLink` (`src/components/ui-link`)         | `crm`  | Canonical link behavior.                                                                          | States `rest/hover/active/disabled` styled from `crm` tokens. **Story 5.1 (#31), BLOCK-01 closed:** `disabled` is now part of the contract (FR-04 acceptance 4, `prd.md:182`). Board A's Disabled column measures `#E1E7EA` on the link rows (`439:19364`, second row `439:19614`) against rest `#969B9D`, with no typography or text-decoration delta, so `ui-link/theme.ts` repaints only the ink, via the `brandGray` token from `ui-color-theme` under `&[aria-disabled="true"]` (plus nested hover/active resets). Semantics keep the anchor's `href` — and therefore its `link` role and accessible name — while adding `aria-disabled="true"`, `tabIndex={-1}` and suppressed activation. Baseline `tests/visual/states.spec.ts` · `link-disabled.png`. **Remaining deviation:** `error` is the sole contract exception left — FR-04 marks `error/invalid` optional and the toolkit contract omits it. |

## Quality Enforcement Checklist — Epic 1 core controls

| Check                                       | UiButton                                                                   | UiInput                                                                                       | UiCheckbox                                                                             | UiLink                                                                                                                    |
| ------------------------------------------- | -------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| Export present in `src/components/index.ts` | ✅                                                                         | ✅                                                                                            | ✅                                                                                     | ✅                                                                                                                        |
| Required state matrix covered               | ✅                                                                         | ✅                                                                                            | ✅                                                                                     | ✅                                                                                                                        |
| Accessibility baseline                      | keyboard + disabled validated (Story 1.3); focus-ring visual deferred (F2) | keyboard + disabled/error + name/error guidance (Story 1.3); focus-width visual deferred (F7) | keyboard + disabled/error validated (Story 1.3); visible focus indicator deferred (F1) | keyboard + focus validated (Story 1.3); `aria-disabled` + tab-order exclusion added (Story 5.1); native focus ring intact |
| Storybook coverage present                  | ✅                                                                         | ✅                                                                                            | ✅                                                                                     | ✅                                                                                                                        |
| Unit tests present in `tests/unit`          | ✅                                                                         | ✅                                                                                            | ✅                                                                                     | ✅                                                                                                                        |
| Provenance entry exists                     | ✅                                                                         | ✅                                                                                            | ✅                                                                                     | ✅                                                                                                                        |

State-parity demonstrability (rest/hover/active/disabled/error visual baselines + prop→semantic
unit assertions) was completed in **Story 1.2** (`#11`). **Story 1.3** (`#12`) hardens the
_behavioural_ accessibility contract — keyboard operability, focus reachability/order, and
consistent disabled/error semantics — and stands up the Epic 1 accessibility test gate
(`tests/unit/core-controls-accessibility.test.tsx`). Per product direction it makes **no colour or
visual changes**: the visible focus-indicator appearance (F1 checkbox — Critical, F2 buttons, F7
input) and all colour/contrast remediation are owned by a dedicated accessibility-visuals follow-up
PR. See `specs/implementation-artifacts/1-3-accessibility-and-interaction-consistency-hardening.md`.

### Epic 2 — Selection, Search, and Input Workflows

| Component                                                           | Source | Rationale                                                                                                                                                                                                                                                  | Alignment notes / deviations                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ------------------------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `UiSearchInput` (`src/components/ui-search-input`)                  | `new`  | No direct `crm`/`website` entry existed; behaviour follows `crm`'s canonical UI library (MUI `Autocomplete`).                                                                                                                                              | Aligned to Figma "Search" (node `439:19393`): `freeSolo` typeahead with optional suggestions, leading magnifier grey→brand-blue on focus, no clear/chevron indicators, `8px` radius, `#D0D4D8` stroke, `#969B9D` placeholder (all `ui-color-theme` tokens). Role `combobox`; `error`→`aria-invalid`, `helperText`→`aria-describedby`, native `required`; dev-only name/error guidance via shared `useDevWarning`. `variant` intentionally omitted (design is outlined-only). Suggestion prefix-highlight deferred (kept MUI default option rendering to avoid a prop-spread lint exception).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `UiSelectWithSearch` (`src/components/ui-select-with-search`)       | `new`  | No direct `crm`/`website` entry existed; behaviour follows `crm`'s canonical MUI `Autocomplete` combobox.                                                                                                                                                  | Aligned to Figma "select с поиском" (node `448:25544`): single-select searchable combobox, `{label,value}` options, clean `onChange(Option \| null)`, custom thin chevron-down popup indicator, `8px` radius and `ui-color-theme` border/focus/error tokens matching `UiInput`. `error`→`aria-invalid`, `helperText`→`aria-describedby`, native `required`; label-less `aria-label` merged into the combobox input; listbox named via `slotProps.listbox`; dev-only name/error guidance via shared `useDevWarning`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `UiCalendarMultiSelect` (`src/components/ui-calendar-multi-select`) | `new`  | No direct `crm`/`website` entry existed; behaviour follows `crm`'s canonical MUI primitives + the WAI-ARIA APG grid pattern. No `@mui/x-date-pickers`/date-library dependency (lean-dependency ethos, as `ui-search-input` avoided `@mui/icons-material`). | Figma calendars (Board B, all 308×321): rest `601:54620`, hover `606:41801`, range selection `606:41904`, cross-month range pair `606:42007`/`606:42218` (the January band continuing into the February endpoint; `606:42007` is also cited at `src/components/ui-calendar-multi-select/style-tokens.ts:24`). Calendar-style multi-select of discrete dates; `value`/`onChange` are ISO `YYYY-MM-DD` string arrays (timezone-safe). Two-level `role="group"` field → `role="grid"` month (`aria-multiselectable`), operable gridcells with explicit `aria-selected` true/false, roving tabindex + full APG keyboard model, `aria-current="date"` today, hidden polite month announcer, `ui-color-theme` tokens (brand-blue selection, grey stroke, danger error border, `8px` radius). **Deviations:** `variant` N/A (documented); `error` conveyed via `role="alert"` helper + `aria-describedby` + border (not `aria-invalid`, unsupported on `grid`); `required` folded into the accessible name + visible asterisk (not `aria-required`, unsupported on `group`); dev-only name/error guidance via shared `useDevWarning`. Selected-day fill contrast / focus-ring pixel hardening follow the same accessibility-visuals deferral as Story 1.3. |
| `UiMultiSelect` (`src/components/ui-multi-select`)                  | `new`  | No direct `crm`/`website` entry existed; behaviour follows `crm`'s canonical MUI `Autocomplete multiple` combobox.                                                                                                                                         | Aligned to Figma "Multiselect" (node `535:37450`): the collapsed field matches "select с поиском" (white, `#D0D4D8` stroke, `8px` radius, `#969B9D` placeholder, thin chevron-down — all `ui-color-theme`), so the shared thin chevron was extracted into `field-controls` (`ChevronDownGlyph`, now used by select + multi-select). `{label,value}` options; controlled `value`/`onChange` are `Option[]`. Selected options render as removable chips (MUI v9 `renderValue` + `getItemProps`, applied explicitly — no prop-spread) with a named delete control ("Remove Kyiv", 24×24 target, `tabIndex=-1`). `disableCloseOnSelect` for multi-pick; listbox `aria-multiselectable` + `aria-selected` toggles; a hidden polite `role="status"` region announces add/remove ("Kyiv added, 2 selected"). `error`→`aria-invalid`, `helperText`→`aria-describedby`; `required` is native only while empty (a filled multi-select must not block submit) — documented exception; dev-only name/error guidance via shared `useDevWarning`. Chip/delete contrast hardening deferred to the accessibility-visuals PR (per Story 1.3).                                                                                                                        |
| `UiFileUploadInput` (`src/components/ui-file-upload-input`)         | `new`  | No `crm`/`website` file-upload existed; the Figma file has **no** dedicated upload component, so the field is composed from the design's own parts (field `193:4763`, progress `269:7159`, tags `345:17479`).                                              | Native `<input type="file">` presented as the Figma bordered field with a pill trigger, extended into a full-field drop target; the upload request stays with the consuming app (`status`/`progress` report it). Always-controlled `files`/`onFilesChange` (documented `value`/`onChange` exception — a file control's value is `File[]`); all-or-nothing validation with actionable messages, re-run in JS for drops (`accept` only filters the OS picker; `accept-matcher.ts` implements `.ext`/`type/subtype`/`type/*`); status pill renders text plus tint (never colour alone); determinate progressbar with clamped `aria-valuenow`; polite `role="status"` announces transitions only; native input clipped (not hidden) so it keeps focus + Enter/Space, pill wears the ring via `:focus-within`; native value cleared after each pick so re-picking the same file fires `change`. **No new colours** — tokens + `alpha()` tints only; drag-over/uploading-pill colours are documented derivations (no dropzone/spinner exists in Figma; `#E6891C` is not a token). `size`/`variant` N/A (documented). Contrast/focus-ring hardening deferred per Story 1.3.                                                                                |
| `UiRadioGroup` (`src/components/ui-radio-group`)                    | `new`  | No direct `crm`/`website` entry existed; behaviour follows `crm`'s canonical MUI `RadioGroup` single-choice control.                                                                                                                                       | Aligned to Figma "radiobutton" (node `151:6441`): a fixed 20px circular radio, white fill — unselected `1px #D0D4D8` stroke (`grey400`), selected `5px #1EAEFF` primary ring (both `ui-color-theme` tokens, no new colour). `{label,value}` options; controlled `value` / clean `onChange(value)` adapted from MUI's `(event, value)` (always controlled via `value ?? ''`, like `UiMultiSelect`). Role `radiogroup` named from `label` (`FormLabel` + `aria-labelledby`) else `aria-label`; native roving-focus arrow-key selection; `error`→`aria-invalid` on the group + red `FormLabel`/helper; `helperText`→`aria-describedby`; native `required` on the radios + MUI group-label asterisk; per-option `disabled`; dev-only name/error guidance via shared `useDevWarning`. **Deviations:** `size`/`variant` N/A (single fixed 20px glyph, like `UiCheckbox`) — documented. Hover/disabled/error states the Figma frame does not spec reuse the established checkbox/multi-select treatments; radio-fill contrast + focus-ring pixel hardening follow the same accessibility-visuals deferral as Story 1.3.                                                                                                                                    |
| `UiPagination` (`src/components/ui-pagination`)                     | `new`  | No `crm`/`website` pagination existed; visuals come from the design's own atoms and composed bar (Ui-kit state atoms `439:19463`–`439:19478`, composed bar `360:12218`), behaviour from the MUI `usePagination` range semantics.                           | A `<nav>` page navigator: prev/next chevron+label links around the 48×48 page-cell row (6px cell gap, 48px group gap, Inter Medium 16/18, 8px radius — all measured 1:1 against Figma, per-state colours from `ui-color-theme` tokens with the 10% hover tint via `alpha()`; the rest-state chevron is pixel-verified grey `grey300`, not ink). Always-controlled numeric `value`/`onChange(page)` (documented number-typed exception); pure `page-range.ts` implements `boundaryCount`/`siblingCount` + start/end ellipsis; the skipped-pages marker is a rest-styled non-interactive `aria-hidden` cell. Page cells are real buttons named "Сторінка N" with `aria-current="page"`; **boundary prev/next self-disable via `aria-disabled` (focusable, inert) so keyboard focus is never dropped to `<body>` (SC 2.4.3)** — native `disabled` is reserved for the whole-bar `disabled` prop; focus-visible uses the calendar's two-layer ring. **Deviations:** `error` N/A (navigation, not a form field); `size`/`variant` N/A (single Figma design) — documented. Contrast-ratio verification stays deferred to the accessibility-visuals PR per Story 1.3.                                                                                      |

State/behaviour is locked by `tests/unit/ui-search-input.test.tsx`,
`tests/unit/ui-select-with-search.test.tsx`, `tests/unit/ui-multi-select.test.tsx`
(+ `tests/unit/multi-select-announce.test.ts`), `tests/unit/ui-radio-group.test.tsx`, `tests/unit/ui-pagination.test.tsx`
(+ the pure-logic suite `tests/unit/pagination-page-range.test.ts`),
`tests/unit/ui-file-upload-input.test.tsx` (+ the direct `accept-matcher` specs), and
`tests/unit/ui-calendar-multi-select.test.tsx` (+ the pure-logic suites
`tests/unit/calendar-multi-select-date-utils.test.ts` and
`tests/unit/calendar-multi-select-logic.test.ts`) — render, accessible name,
error/helper/required semantics, keyboard navigation (combobox for the search/
select/multi-select controls; the full APG grid model for the calendar; roving-focus arrow selection for the radio group), selection,
chip removal + status announcements, disabled tab-order, and the dev-warning
contract — at 100% coverage. Visual baselines for the default stories are committed
under `tests/visual`.

### Epic 3 — Data Presentation and Cards

| Component                                                       | Source | Rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Alignment notes / deviations                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `UiItemRow` (`src/components/ui-item-row`)                      | `new`  | No `crm`/`website` item-row existed (the repo's `ui-card-item`/`ui-card-list` are the unrelated marketing-site cards); the row is composed from the Figma "atom switcher" REST-endpoint masters on the "Docs Rest API" screens (GET `439:19720`, PUT `439:19722`, POST `439:19723`, DELETE `439:19724`, muted/GREY `439:19721`), with interaction from the WAI-ARIA APG **disclosure** pattern on `crm`'s canonical MUI primitives.                                                                                                                                                                                                                                                                                                   | Aligned 1:1 to the atom-switcher masters: 52px row, `8px` radius, `1px` accent border over an accent-at-10% (`alpha()`) tint; white badge pill (Golos DemiBold 16/26, per-method accent ink, `0 8px 13.5px` per-method drop shadow); path Golos DemiBold 18 `#1A1C1E` (`darkPrimary`); description Golos Medium 15/18 `#404142` (`grey200`); trailing chevron (`#1B2327` `darkSecondary`; `#E1E7EA` `brandGray` on grey rows) + a decorative open padlock (`#57595B` `grey250` — the only lock glyph in the whole Figma file, identical on every use, so it is `aria-hidden` and adds nothing to the name). Hover darkens the accent border/badge ink to the new `getMethodHover #0091E2` / `putMethodHover #DD9F00` / `postMethodHover #00AE70` / `deleteMethodHover #C72C2C` tokens (added to `ui-color-theme`) plus a per-method `0 4px 9px` row shadow (hover masters `439:19772`–`439:19776`, grey hover `439:19773`); the expanded state (header `192:8298`) flips the chevron up and tints it to the method accent. Muted/inactive status (Figma GREY `439:19721`): shaded `#f4f5f6` (`backgroundGrey200`) fill, `brandGray` border, `grey300`/`grey400` inks, hover ink `#1C2022` (`mutedInkHover`) — exposed via the repo `aria-disabled` boundary pattern (still a focusable `<button>`; `onToggle` no-ops while muted; native `disabled` is never set). Mobile (`≤480px`, CSS-only, same DOM) is a two-line layout switching Golos→Inter (path 16/18, desc 12/18), 20px icons, and a transparent badge whose per-method shadow is re-expressed as a `drop-shadow` **filter** (not `box-shadow`) to match Figma's no-fill frame effect (mobile master `205:7840`). **APG disclosure, always controlled:** with `onToggle` the whole row is one native `<button type="button">` carrying `aria-expanded` (never self-flips — the next state is fed back through `onToggle`, like `UiPagination` feeds pages), and `aria-controls` (`panelId`) is surfaced ONLY while expanded; without `onToggle` the row is static, non-focusable content (no button role, no `aria-expanded`). Accessible name comes straight from the visible text ("{METHOD} {path} {description}"). The chevron glyph is a new shared-style `Glyph` baked at the Figma export's `1.667@20` stroke weight (the existing `field-controls` `ChevronDownGlyph` is `1.5` and did not match the export). **Deviations:** the details-_panel_ content is out of scope for 3.1 (future story); dev-only guidance via shared `useDevWarning` when `expanded`/`panelId` arrive without `onToggle`; `size`/`variant` N/A. Contrast/focus-ring pixel hardening follows the same accessibility-visuals deferral as Story 1.3.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `UiItemsList` (`src/components/ui-items-list`)                  | `new`  | No `crm`/`website` list container existed (the repo's `ui-card-list` is the unrelated marketing-site card list); a pure composition wrapper that stacks `UiItemRow` children, matching the Figma "Frame 152" row usage (node `187:7990`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | A semantic `<ul role="list">` with one `<li>` per row (the row is the list item's sole child), `8px` apart, full width, and no chrome of its own — the explicit `role="list"` is a sanctioned redundant-ARIA exception because Safari/VoiceOver strip list semantics from a `list-style: none` list. Adds no interactive behaviour (no keydown, no tabindex); an **empty** collection renders NOTHING (no `<ul>`, so a `list` role never appears for a zero-row collection). The `8px` gap is measured from the "Frame 152" list usage (`187:7990`), where rows sit on a 60px pitch (52px row + 8px gap). Optional `aria-label` names the list (not a landmark, so no default). Composition-based — children are `UiItemRow` elements and each is wrapped in its own list item; children are flattened through React Fragments so a fragment of rows still yields one `<li>` per row.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `UiTaskCard` (`src/components/ui-task-card`)                    | `new`  | No `crm`/`website` task/person card existed (the repo's `ui-card-item`/`ui-card-list` are the unrelated marketing-site cards); the card is composed 1:1 from the Figma UI-kit "Cards" task masters (Rest `439:19884`, Hover `439:20208` — the design's only two states), with interaction as a plain native action button (fire-and-forget `onActivate` — deliberately **not** the 3.1 APG disclosure) on the toolkit's established MUI primitives and boundary patterns.                                                                                                                                                                                                                                                             | Aligned 1:1 to the 372×94 task masters: a full-bleed bottom-divider row card (`1px` `#E1E7EA` `brandGray`, no radius, transparent background — cards stack flush at a 94px pitch in board columns), `14px 16px` padding, CSS grid with a reserved 34px avatar track + 12px gap. 34×34 circular **informative** assignee photo (`alt = assignee.name`; the paired `TaskAssignee { name, avatarSrc }` object makes a nameless photo unrepresentable; omitted `assignee` = supported unassigned state — track stays reserved, nothing painted, no initials placeholder because the design has none). Title Inter Medium 16/18 `#404142` (`grey200`), natural wrap (`minHeight`, never `height`/clamp/ellipsis — SC 1.4.4/1.4.10); meta row 8px below: required `deadlineLabel` prop (no baked-in natural-language literal, SC 3.1.2) Inter Medium 14/18 `#969B9D` (`grey300`) + deadline chip (`brandGray` fill, 4px radius, `2px 4px` padding, `#1A1C1E` `darkPrimary` 14/18 ink, `1px` transparent border at rest for hover no-jitter). Hover (master `439:20208`, gated `:not([aria-disabled="true"])`): title → `darkPrimary`, label → `grey200`, chip → white fill + `brandGray` border + `0 4px 2px rgba(174, 181, 186, 0.25)` shadow (raw literal per the 3.1 recipe convention; `box-shadow`, not filter — the chip is an opaque box). **Wired/static split:** `onActivate` present → the whole card is one native `<button type="button">` with a content-derived accessible name "{assignee.name} {title} {deadlineLabel} {deadline}" (no `aria-label`; the `@mention` stays plain text — a nested link would be invalid inside a button); absent → static `<div>`, identical content tree. `disabled` uses the repo `aria-disabled` boundary (focusable, model-layer no-op, native `disabled` never set — SC 2.4.3); Figma ships **no disabled visual** — recorded escalation, semantics-only (suppressed hover + `cursor: default`). Focus: two-colour **both-inset** ring (`inset 0 0 0 2px darkPrimary` + `inset 0 0 0 4px white` — outset rings would be clipped by scrolling board columns) + a `forced-colors` outline branch; declared after hover so source order wins. Zero live regions in any state. **Deviations:** `value`/`onChange` N/A (no state fed back — `onActivate` is the analogue); `error`/`size`/`variant` N/A (single master, fluid width); no `aria-expanded`/`aria-controls` (not a disclosure); no heading role, no self-`<li>` (column heading/list wrapping belong to the consumer); no roving tabindex (a column is a list of independent buttons). Label-rest contrast (2.81:1) routed to the accessibility-visuals PR per the Story 1.3 deferral.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `UiProfileSelectCard` (`src/components/ui-profile-select-card`) | `new`  | No `crm`/`website` profile or menu card existed (the repo's `ui-card-item`/`ui-card-list` are the unrelated marketing-site cards); the card is composed 1:1 from the Figma UI-kit "Cards" profile masters in the Cards frame `439:19893` (Rest `451:26219`, Hover `451:26229`, Active/open `451:26252`, Disabled `451:26255` — the design's four states), with interaction from the WAI-ARIA APG **menu button** pattern on the toolkit's established MUI primitives and boundary patterns — deliberately **not** the 3.1 disclosure (the rows are fire-and-forget commands, not revealed inline content) and **not** a combobox (no value axis, no `aria-selected`).                                                                 | Aligned 1:1 to the 225×48 profile masters: a white `8px`-radius trigger with a `1px` `#D0D4D8` (`grey400`) border, fluid width and `minHeight: 3rem` (the consumer sizes it; stories and board tiles render the master's 225px). Asymmetric `6px 12px 8px 7px` padding over a **constant** 1px border compensates Figma's inside-stroke: the 32px circular photo lands at outer (8, 7) — the master's 1px-above-centre offset — the Inter Medium 14/18 `#1A1C1E` (`darkPrimary`) name at outer x=50, and the 20×20 `grey300` chevron box at outer (192, 14) via a `2px` `marginTop`, because Figma centres the chevron but not the avatar/name. The chevron **never flips** when the menu opens: open reaches AT through `aria-expanded` alone and the design invents no substitute visual. Hover (master `451:26229`, gated `:not([aria-disabled="true"]):not([aria-expanded="true"])` so an open card keeps Figma's plain rest chrome) steps the border `grey400` → `grey300` and adds the "Landing shadow" `0 8px 27px rgba(49, 59, 67, 0.14)` (raw literal per the 3.1 recipe convention; `box-shadow`, not filter — both surfaces are opaque boxes). Disabled (`451:26255`): `brandGray` fill, `grey300` name, half-opacity photo, and the 1px border merely turns **transparent** so the fill paints under it — Figma's borderless disabled frame at zero geometry cost. Menu (`451:26252`): white, `1px grey400`, `8px` radius, Landing shadow, 11px below the trigger, clipped by `overflow: hidden`, **not** portalled and **unmounted** when closed (never `display: none`); `−1px` margins slide the first/last rows back under the real border, so the master's 136px outer height and 0/46/92 row pitch survive a CSS outside-drawn border without shrinking rows that must keep `min-height: 44px` (SC 2.5.8) and still grow when a label wraps (SC 1.4.4/1.4.10). Rows are native `<button type="button" role="menuitem" tabindex="-1">` **direct children** of `role="menu"`; their `#F4F5F6` (`backgroundGrey200`) hover fill is decoration and is **never** the focus indicator. **APG menu button, always controlled:** `onOpenChange(next)` present → the trigger is one native `<button type="button">` with `aria-haspopup="menu"`, a permanent `aria-expanded`, and `aria-controls` surfaced ONLY while the menu is mounted (the 3.1 `panelId` precedent); absent → a static `<div>` with no role, no `tabindex` and no ARIA of any kind, and the menu never renders. Ownership split: the consumer owns `open`, the **component owns focus** — focus always enters the menu on open (pointer and keyboard alike; `first`, or `last` only for ArrowUp), returns to the trigger synchronously on Escape and on activation, and is rescued when the menu is closed underneath it, so it is never dropped to `<body>` (SC 2.4.3). `aria-activedescendant` and roving `tabindex="0"` are forbidden — the menu contributes zero tab stops; empty `items` renders no menu at all (the `UiItemsList` empty-renders-nothing rule). `disabled` uses the repo `aria-disabled` boundary (focusable, native `disabled` never set) and **dominates** `open`: closed presentation, every open path a no-op, no self-emitted `onOpenChange(false)`. Focus ring: **single-layer** `inset 0 0 0 2px darkPrimary` on trigger and rows alike (this card paints its own white fill, so 3.2's second white layer buys nothing) plus a `forced-colors` outline branch. The photo is **decorative** (`alt=""`), diverging from `UiTaskCard`'s informative avatar by the contract's own ruling — the name beside it is already the trigger's accessible name. **Deviations:** `value`/`onChange` N/A (`onOpenChange`/`onSelect` are the analogues); `error`/`size`/`variant` N/A (a single master; rest/hover/open/disabled are states, not variants); `onOpenChange` carries a boolean unlike 3.1's bare `onToggle()` (five close paths race a payload-free toggle); typeahead out of scope, on record. Contrast/focus-ring pixel hardening follows the same accessibility-visuals deferral as Story 1.3.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `UiIntegrationCard` (`src/components/ui-integration-card`)      | `new`  | No `crm`/`website` integration/brand card existed (the repo's `ui-card-item`/`ui-card-list` are the unrelated marketing-site cards); the card is composed 1:1 from the Figma UI-kit "Cards" integration masters in the Cards frame `439:19893` (Rest `451:26264`, Hover `451:26277`, Selected/"Active" `451:26269` — the design's three states), with interaction from the WAI-ARIA **`radio`** role semantics on the toolkit's established native-button, wired/static and `aria-disabled`-boundary conventions — deliberately **not** `aria-pressed` (a toggle does not imply the mutual exclusivity the radio glyph promises) and **not** a self-contained radio group (the group is the consumer's).                              | Aligned 1:1 to the 312×142 masters: a white **radius-12** card (this card's own chrome — the task/profile cards are r8) with a **constant 1px** border swapping colour only — `brandGray` rest → `grey400` + "Landing shadow" `0 8px 27px rgba(49, 59, 67, 0.14)` on hover → `primary #1EAEFF` + retained shadow when selected (no-jitter, the 3.2/3.3 lineage); header row at outer (16, 15.5) holding a 20px **`aria-hidden` CSS radio glyph** reproducing the `radiobutton` masters (`151:6437`/`151:6438`) byte-identically to `UiRadioGroup`'s recipe (unchecked `1px grey400`, checked `5px primary` ring — never an `<input>`, never focusable, never a nested control) riding the first text line at +3, then the Golos Text 400 16/26 `darkPrimary` brand name — the card's whole accessible name (no `aria-label` anywhere, WCAG 2.5.3). The brand logo is a **decorative** (`alt=""`) required `{ src, width, height }` bundle (a dimensionless logo is unrepresentable — the `TaskAssignee` trick), horizontally centred with its vertical seat computed from its own height (`logoTop = (142 − h/2)/2`, both masters within 0.5px), `max-width: 100%; height: auto` for reflow. **Wired/static split on `onSelect`:** present → the whole card is ONE native `<button type="button" role="radio">` with a permanent `aria-checked` mirroring the always-controlled `selected` (coerced nullish, never self-flipped; activation of an already-selected card fires nothing — native radio `change` semantics; a declined selection stays eligible); absent → a static `<div>` with no role, no `tabindex`, no ARIA, painting the rest presentation even if `selected` is passed (dev-warn). A wired card outside a `[role="radiogroup"]` ancestor dev-warns (ARIA 1.2 requires no context role, so standalone stays valid — AC3); `aria-setsize`/`posinset`/roving-tabindex belong to a future group story, on record. `disabled` uses the repo `aria-disabled` boundary (focusable, model-layer no-op, native `disabled` never set — SC 2.4.3); Figma ships **no disabled visual** — recorded escalation, semantics-only, and a selected+disabled card keeps its full selected chrome (the `UiRadioGroup` preserve-the-indicator precedent). Hover is gated `:not([aria-disabled="true"]):not([aria-checked="true"])` — **selected dominates hover** (no such master exists; held by a dedicated selected+hover baseline). Focus: single-layer `inset 0 0 0 2px darkPrimary` ring emitted as a two-selector list so it also outranks the hover rule's specificity on a focused+hovered card (Amendment A1), plus a `forced-colors` outline branch; checked-vs-unchecked survives forced colors through border **width** (5px vs 1px), a geometry channel that must not be refactored away. Zero live regions, zero key handlers, zero `.focus()` calls, no animation. **Deviations:** `value`/`onChange` map to `selected`/bare `onSelect()` (one path, one direction — the inverse of 3.3's payload rationale); `error`/`size`/`variant` N/A (documented). Selected-chrome contrast (`primary` 2.46:1) and the unchecked `grey400` stroke (1.49:1) are inventoried for the accessibility-visuals PR per the Story 1.3 deferral, alongside `UiRadioGroup`'s identical class.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `UiFilterChip` (`src/components/ui-filter-chip`)                | `new`  | No `crm`/`website` filter chip existed (the `UiMultiSelect` chips are Autocomplete-internal delete affordances, not a standalone filter pill); the chip is composed 1:1 from the Figma Board A "Tags" master `397:19014` (Rest `439:19370`, Hover `439:19372`, Active `439:19373`, Disabled `439:19371` — the design's four states, whose child geometry `get_metadata` returns byte-identical in every column), with interaction as a plain native action button on the toolkit's established wired/static and `aria-disabled`-boundary conventions — deliberately **not** a nested 24px x-button inside a non-interactive pill (Ruling 6: that shrinks the target for no reason and makes one action a two-element tab experience). | Aligned 1:1 to the 256×30 "Tags" masters: an `inline-flex` pill at `0.25rem` radius with `align-items: flex-start` (A3 — the 18px label row and the 20px glyph box both start at y=5, so the label's optical centre sits 1px above the glyph's exactly as Figma draws it; `center` would diverge from the pixels), `4px 7px` padding over a **constant** `1px solid transparent` border (Figma strokes inside, so the hover/active border must neither grow the box nor move the children — the no-jitter law), `minHeight: 1.875rem`, and a `white-space: nowrap` width that hugs its contents (256px is only what the sample string measures; truncation is out of scope, Figma specifies none). Figma paints the label as two differently-coloured segments, so they are two props rather than one string (Q9): `label` (Inter 500 14/18 `grey250`) then `filterValue` (`darkPrimary`), both swapping to `grey300` when disabled as an explicit **per-segment** override, never inheritance. Per-state chrome is colour-only over frozen geometry — `grey500` fill at rest and disabled → `white` + `grey400` border + `CHIP_SHADOW` on hover → `white` + `grey300` border on active — with the glyph ink stepping `grey300` → `primary` → `containedButtonActive`; opacity is 1 in every state, disabled swaps colours rather than dimming. `CHIP_SHADOW = '0 4px 4px rgba(26, 27, 36, 0.09)'` is the module's one tolerated raw literal (`#1A1B24` is six blue-channel steps off `darkPrimary`, so `alpha()` would shift the channel and break the baseline) emitted as `box-shadow`, not Figma's `drop-shadow()` filter — pixel-equivalent on an opaque rounded rect, cheaper, and it creates no containing block. The × is a **new** 20-unit `X_CLOSE_PATH` rendered through the shared `field-controls` `Glyph` at `strokeWidth="1.667"` — explicitly **not** the stock Untitled-UI `x-close`, whose arms are one 24-unit step longer each side; no SVG asset is committed. **Wired/static split on `onRemove`:** present → the whole 30px pill is ONE native `<button type="button">` (SC 2.5.8 passes on the full pill) whose accessible name is content-derived in DOM order — both visible segments, then a visually-hidden `srOnlySx` suffix (default `', видалити фільтр'`, overridable via `removeLabel`) — with `aria-label` **forbidden** anywhere in the tree; absent → a static `<div>` with no role, no `tabindex` and no ARIA, the × still painted decoratively (the `UiItemRow` chevron precedent). No `aria-pressed`/`aria-checked`/`aria-expanded` — a plain action button. `disabled` uses the repo `aria-disabled` boundary (focusable, model-layer no-op, native `disabled` never set — SC 2.4.3), and because Figma ships a disabled column it is painted rather than escalated. Focus: the Amendment-A1 two-selector `inset 0 0 0 2px darkPrimary` ring plus a `forced-colors` outline branch. Zero live regions, zero key handlers (native Enter/Space only). **Deviations:** `value` N/A (a chip has no value axis — its existence is its state) and `onChange` maps to a bare, payload-free `onRemove()`; `error`/`size`/`variant` N/A, `lang` supported (filter text is user content) — all documented. The chip never moves focus on removal (it unmounts): the `types.ts` JSDoc binds the **consumer** to move focus to the next chip, the previous chip or the filter-region heading, or it drops to `<body>` (SC 2.4.3). Rest (2.42:1) and hover (2.46:1) glyph contrast and the 1.49:1 hover stroke are inventoried for the accessibility-visuals PR per the Story 1.3 deferral.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `UiPinInput` (`src/components/ui-pin-input`)                    | `new`  | No `crm`/`website` one-time-code field existed and `UiInput` is a single free-text field; the control is built from the Figma Board A "2FA item" component-set master `72:5172` (Rest `439:19615`, Hover `439:19617`, Active `439:19619`, Disabled `439:19623`), whose 64×86 frame **is one digit cell** — the design ships no group, no gap and no separator — with behaviour from native per-cell `<input>` semantics plus the shared `field-controls` error/helper contract; one grouped input was rejected because the design paints discrete cells and per-cell inputs are what give a correct caret, per-digit announcement and native forms-mode behaviour.                                                                    | Aligned 1:1 to the 64×86 cell: `4rem` × `5.375rem` (`minHeight`), `0.75rem` radius, a **constant** `1px` border whose colour alone swaps, and a flex-centred Golos Text 700 22/26 digit (Figma's `calc(50% − 0.5px)` offsets are sub-pixel rounding and are deliberately not ported). Chrome: `white` fill with a `brandGray` border at rest, `grey400` on hover, `brandGray` + `PIN_FOCUS_SHADOW` when focused, and `grey500` fill **with a `grey500` border** when disabled — Figma genuinely deletes that stroke, but porting it literally would shift the content box 1px per edge, and `grey500`-on-`grey500` is pixel-identical to a borderless `#EAECEE` cell (verified by pixel histogram). `PIN_FOCUS_SHADOW = '0 7px 12px rgba(76, 90, 126, 0.15)'` is the module's one tolerated raw literal (`#4C5A7E` has no near token — the nearest opaque `grey250` is ΔE ≈ 12 and hue-wrong, neutral vs blue-violet) and is **distinct** from `ui-item-row`'s `LANDING_SHADOW` and `ui-filter-chip`'s `CHIP_SHADOW`; bleed was verified independently (the active node renders 88×110 against a 64×86 box while rest, hover and disabled render at exactly 64×86 → no shadow on any state but focus). Board A's "Active" column **is** the focused cell, so Figma's caret+shadow ship _in addition to_ the Amendment-A1 ring (Figma specs no focus ring, and a caret alone is not a 3:1 indicator). **Structure:** N native `<input type="text" inputMode="numeric" pattern="[0-9]*" maxLength={1}>` cells (never `type="number"` — spinners, wheel mutation, locale-dependent value) inside a `role="group"` named by `label` or `labelledBy` (`labelledBy` wins), `autoComplete="one-time-code"` on the **first cell only**, each cell named from `cellLabel(index, length)` (default `Цифра N з M`, 1-based; no visible per-cell text, so `aria-label` is legal). Always-controlled `value`/`onChange(next: string)` carrying the whole concatenated code, coerced `?? ''`, filtered to digits, clamped to `length` and kept **dense** by construction (a digit typed past the end lands at the end rather than opening a hole); the component holds **no React state** — its only internal state is the DOM focus target in refs. There is deliberately **no `onComplete`** (a second callback on the same keystroke races `onChange`; consumers compare `next.length === length`). Exhaustive keyboard contract: a digit fills and advances (never past the last cell) while non-digits are rejected outright, Backspace clears in place then retreats-and-clears on an empty cell, Delete clears in place, arrows move, cells select their content on focus so typing overwrites, and paste strips non-digits, distributes from the focused cell, truncates at `length` and lands focus after the last cell filled. **All cells stay in the natural tab order — no roving tabindex** — because real inputs must remain reachable in screen-reader forms mode; arrows are a convenience layer, not the only path. Autofill and typed entry hit the same validation path (the 2.4A lesson). `error` → `aria-invalid` on **every** cell with one `FormHelperText` linked from every cell via `aria-describedby` (per-cell announcement is intentional; the helper is the non-colour error signal, and `error` without `helperText` dev-warns); `required` → `aria-required` on the **first cell only** (one announcement, not N). **Deviation (Ruling 3):** disabled uses `readOnly` + `aria-disabled="true"` instead of the button-shaped no-op boundary — the same SC 2.4.3 intent with the input-appropriate mechanism, native `disabled` still forbidden, and the `role="group"` wrapper carries no `aria-disabled` (it is not a widget role). **Rulings by omission:** the 12px inter-cell gap (Q1, not exposed as a prop — there is no variant axis), the `grey400` placeholder / `darkPrimary` entered-digit ink (Q2 — all four masters show the same grey "0"), and a native `caretColor: primary` caret standing in for Figma's painted 2×26 bar (Q3 — a painted caret would double-draw against the browser's own). `size`/`variant` N/A, `lang` N/A (digits are language-neutral) — documented. Placeholder (1.49:1) and rest-border (1.25:1) contrast are inventoried for the accessibility-visuals PR per the Story 1.3 deferral.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `UiPaymentOptionCard` (`src/components/ui-payment-option-card`) | `new`  | No `crm`/`website` payment-method card existed; the card is composed 1:1 from the Figma Board A payment frames 137–140 (Rest `439:19625`, Hover `439:19643`, Selected/"Active" `439:19640`, Disabled `439:19658` — the design's four states), with interaction from the WAI-ARIA **`radio`** role semantics established by `UiIntegrationCard` (3.4) and reusing that component's `IntegrationLogo` type across a **type-only** dep-cruiser edge rather than cloning it — deliberately **not** `aria-pressed` and **not** a self-contained radio group (the group, its name and its set metrics are the consumer's).                                                                                                                  | Aligned 1:1 to the 279×90 masters: a fluid `100%`-wide card at `minHeight: 5.625rem` and `0.5rem` radius, laid out as a `position: relative` box with an absolutely-positioned selection circle and a **card-axis-centred** logo layer — Figma writes `left: calc(50% + 0.5px)` on the mark, so a naive flex row would push it ~16px off-centre. **Brand ≠ state:** only the active master carries WAYFORPAY, a content swap showing a second provider, so every measurement splits into STATE chrome (card + circle) and BRAND content (LiqPay 116×24, WayForPay 187×67, the latter cropped to the ~50px of padding Figma actually renders — tightening to the ink box grows the mark ~8%). Figma's 0/1/2px border ladder is **not** shipped literally (it would reflow the content box by 2px between rest and selected): the card always declares `1px solid`, colour-only — `backgroundGrey200` at rest and disabled (invisible against the identical fill), `primary` on hover and when selected — and the selected state adds its second pixel as an `inset 0 0 0 1px primary` box-shadow layer the focus ring then composes with, called out in the `styles.ts` header the way 3.4 does. Card fill runs `backgroundGrey200` → `white` on hover (Q6 — Frame 139 declares no fill at all; `white` is ruled because hover is the transitional step toward the explicitly-white selected state, and a transparent card would inherit the consumer surface and break the progression) → `white` when selected → `backgroundGrey200` when disabled; no state has a shadow. The 20px selection circle reuses `UiIntegrationCard`'s radio-dot recipe verbatim — **checked is border WIDTH (1px → 5px), not colour**, a geometry channel that survives forced colors and must not be refactored away — with one divergence: the disabled circle is a solid `brandGray` disc with no stroke where 3.4 keeps a stroked glyph. The disabled wordmark is an **asset swap, not a filter** (Q7: `grayscale(1)` and `opacity` both miss Figma's `#D0D4D8` badly), so `logoDisabled` is an optional prop falling back to `logo` and a provider with no grey variant still renders sensibly. **Wired/static split on `onSelect`:** present → the whole card is ONE native `<button type="button" role="radio">` with a permanent `aria-checked` mirroring the always-controlled `selected` (coerced nullish, never self-flipped; activating an already-selected card fires nothing — native radio `change` semantics; a declined selection stays eligible); absent → a static `<div>` with no role, no `tabindex` and no ARIA, painting the rest presentation even if `selected` is passed (dev-warn). A wired card with no `[role="radiogroup"]` ancestor dev-warns via the 3.4 `closest()` mount effect; `aria-setsize`/`aria-posinset`/roving tabindex belong to a future group story, on record. **Mandatory deviation from 3.4 (Ruling 1):** this card has **no visible text** — the wordmark image is its only content — so copying 3.4's decorative `alt=""` logo verbatim would produce a nameless radio (SC 4.1.2, Critical). The `<img>` therefore carries `alt={name}` and **is** the entire accessible name, `name` is required and must transcribe the visible wordmark ("LiqPay", "WayForPay" — SC 2.5.3), and `aria-label` is forbidden anywhere in the tree. The circle stays `aria-hidden` CSS paint, never an `<input>`, never focusable, and the selected ring keys off `[aria-checked="true"]` in styles — never off a class — so the static branch cannot paint it by construction. `disabled` uses the repo `aria-disabled` boundary (focusable, model-layer no-op, native `disabled` never set — SC 2.4.3) and is painted because Figma ships the column; selected+disabled keeps the full selected chrome, and **selected dominates hover** (no such master exists — a selected+hover baseline pins it pixel-identical to selected). Focus: 3.4's Amendment-A1 two-selector `inset 0 0 0 2px darkPrimary` ring plus a `forced-colors` branch, so a focused selected card shows the blue border and the ring at once. **Deviations:** `value`/`onChange` map to `selected`/bare `onSelect()`; `error` N/A (validation belongs to the consumer's group), `size`/`variant` N/A, `lang` N/A (brand wordmarks are proper nouns) — documented. Selected-ring (2.46:1) and rest circle-stroke (1.49:1) contrast join `UiIntegrationCard`'s identical class in the accessibility-visuals PR per the Story 1.3 deferral.                                                                                                                                                                                                                                                                                                                                       |
| `UiActionIconBar` (`src/components/ui-action-icon-bar`)         | `new`  | No `crm`/`website` icon bar existed; the bar is derived from six loose, hand-placed Figma Board A instances parented directly to the frame (`x-close` `439:19830`, `dots-horizontal` `439:19860`, `dots-vertical` `451:25809`, `eye`/`eye-off` `451:25817`, `settings-04` `451:26186`, `trash-02` `632:46703`, each with its own hover/active/disabled node) — **there is no auto-layout container, therefore no Figma-authored gap or padding token** — with interaction as a set of independent native buttons under `role="group"`, deliberately **not** `role="toolbar"` (Ruling 2).                                                                                                                                              | Layout here is **derived, not authored** — the single biggest judgment call in 3.5. The measured slot-box gaps (12/4/5/10/12) are the arithmetic consequence of six glyphs with 1.16→10px of differing internal padding eyeballed onto one optical rhythm; only the ink-gap series clusters (mean 16.66, median 16.5, σ ≈ 2.1), and the vertical ink centres land on one centreline ±1px of board noise. **Ruling (Q4):** a flex row at `gap: 0.75rem` — the modal measured gap, which reproduces `x-close`→`dots-horizontal` and `settings-04`→`trash-02` exactly and normalises the two collapsed gaps that read as a board mistake — over uniform `1.5rem` slots with no border, background or radius at rest; the board's 4px and 5px gaps are deliberately NOT reproduced. `settings-04` is normalised from 30×30 to 24×24 by rendering its native 30-unit viewBox in a 24px box, where its 2.5 stroke resolves to exactly 2 (Q11). Across all four state columns **nothing but stroke colour changes** (paths hash-identical with the stroke attribute stripped, geometry and opacity frozen): the neutral lane (x-close, both dots, settings) steps `grey300` → `primary` → `containedButtonActive` → `grey400`; the eye steps `grey300` → `grey200` — Figma's deliberate anomaly, kept, because a visibility toggle is a neutral affordance, not a primary action; the danger lane steps `error` → `strokeDanger` and adds the **only authored button chrome anywhere on Board A**, `Frame 5441` (`632:46709`): a 40×40 `alpha(error, 0.1)` backdrop at `0.5rem` radius, painted as an absolutely-positioned `aria-hidden` class-hooked `2.5rem` layer behind the 24px glyph so the slot rhythm never reflows (the flattened `#FBEBEB` is deliberately not hardcoded — it is only correct over white). Seven `currentColor` path constants ship inline in `icon-paths.ts`; because `dots-horizontal`/`dots-vertical` (3 subpaths) and `eye` (2) exceed the shared `field-controls` `Glyph`, that primitive was extended **additively** to accept `path: string \| readonly string[]` — one change, six call sites, no behaviour change for existing callers, covered by a new array-path test — rather than hand-rolling a second SVG primitive. **Role (Ruling 2): `role="group"` named from a required bar `label`, NOT `role="toolbar"`.** The toolbar role contractually promises arrow-key roving-tabindex navigation; this repo has zero roving precedent, 3.4 explicitly rejected composite focus managers, and with at most six controls tab-stop reduction buys almost nothing — a role without the behaviour would be a Critical defect, so a toolbar upgrade is a whole future story or nothing. Every action is therefore an independent tab stop in DOM order: a native `<button type="button">` named by a required per-action `label` (`aria-label`; icon-only, so SC 2.5.3 does not bind) over an `aria-hidden focusable="false"` glyph. Plain actions carry **no state ARIA** — the trash's red backdrop is a pointer state, never a toggle. The eye is the one toggle: `aria-pressed` in both states from an always-controlled `pressed`, with a **constant** label ("Показати"/"Приховати" alongside `aria-pressed` double-signals and reads contradictorily) and its eye→eye-off swap as the component's only non-colour state cue, preserved deliberately; Figma's disabled column also shows `eye-off`, a copy-paste artefact, so disabled renders whichever glyph the toggle is actually in. The dots actions take an optional popup passthrough (`hasPopup` → `aria-haspopup="menu"`, `menuOpen` → `aria-expanded` in **both** states, `menuId` → `aria-controls` **only while the menu is mounted** — the 3.3 dangling-idref rule); the bar owns no menu of its own. `disabled` is per-action **and** whole-bar through the repo `aria-disabled` boundary (focusable, never removed from the tab order, native `disabled` never set — SC 2.4.3). Static per S2: an unwired action renders a plain `<span>` and a fully unwired bar drops the group role and name entirely, both branches rendering the identical tree. Focus: Amendment-A1 two-selector ring per button plus a `forced-colors` branch. **Deviations:** `value`/`onChange` N/A at bar level (per-action callbacks; the eye's axis is `pressed`/`onToggle`), `error`/`size`/`variant`/`lang` N/A — documented. The eye's pointer-`:active` colour is **invented by extension** (Q5 — Figma ships no such cell, and without it the eye would be the one control with no press feedback). Rest (2.81:1), hover (2.46:1) and destructive (2.96:1 / 2.56:1) glyph contrast is a design-source defect escalated to the designer and routed to the accessibility-visuals PR per the Story 1.3 deferral; the 1.15:1 backdrop is never relied on as a sole state indicator. |
| `UiStatusBadge` (`src/components/ui-status-badge`)              | `new`  | No `crm`/`website` status marker existed; the badge is composed 1:1 from the Figma Board A check-circle frames 419/5416–5418 (Rest `451:25843`, Hover `451:25849`, Active `451:25845`, Disabled `451:25852` — the design's four states), with a **dual-mode** contract (Ruling 4): static `role="img"` by default, a native `aria-pressed` toggle button when `onToggle` is wired — deliberately **not** `role="switch"` and **not** `aria-checked` (the inverse of the 3.4 radio ruling on the same reasoning: a toggle implies neither a switch's on/off channel nor a radio's mutual exclusivity).                                                                                                                                 | Aligned 1:1 to the 26×26 master: one circular container (`1.625rem`, `'50%'` pinned in styles and in the style test with Figma's literal `54px` recorded in a comment, Q12) holding a flex-centred `1rem` glyph frame, and **every delta across all four states is colour-only** — box size, radius, glyph geometry, path, stroke weight, position and opacity are byte-identical. Chrome: `white` fill with a `brandGray` border and check at rest, `alpha(success, 0.1)` fill with a `success` border and check on hover, solid `success` with a white check when active, and `brandGray` with a white check when disabled — so **disabled derives from ACTIVE, not from rest**, a fact pinned in the API docs. **J1:** Figma drops the stroke on active and disabled, but because it strokes inside, all four masters render at exactly 26×26 with the glyph at (5, 5) — so the `2px` border is always emitted and only its colour swaps, pixel-identical to no border and no-jitter by construction; it is never conditionally added or removed. The check is one three-point path at **1.6× the standard Untitled-UI weight** (`strokeWidth="3.2"` at viewBox 24 → 2.1333px rendered): it is what the design renders and what the export carries, so it _is_ the spec — deliberately not "corrected" to 2 and not `UiItemRow`'s `1.667` — and it is rendered through the shared `Glyph` with the full 24-unit viewBox retained, which is what reproduces the leaf's ~0.33px optical rise above true centre automatically. **Dual mode on `onToggle` (Ruling 4).** Static (the default): a `<span role="img" aria-label={label}>` over the `aria-hidden` SVG, no tabindex and no other ARIA, where the **required** `label` is the entire non-visual signal and MUST name the state being painted ("Завдання виконано" / "Завдання не виконано") — that label is the colour-only mitigation (SC 1.4.1, 1.1.1), and `role="img"`'s name being the exposure channel is precisely why **static + `active` is legal** here where 3.4's unwired `selected` is not. Interactive: ONE native `<button type="button">` with `aria-pressed` from the always-controlled `active` (coerced nullish, never self-flipped) and a **constant**, state-free `label`, because a state-describing name plus `aria-pressed` double-signals. `disabled` uses the repo `aria-disabled` boundary (focusable, model-layer no-op, native `disabled` never set — SC 2.4.3); active+disabled keeps the active chrome exactly as Figma draws it. The 26px box is the SC 2.5.8 hit area and must not be shrunk. Focus: the Amendment-A1 two-selector ring whose second copy repeats the hover gate's `:not([aria-pressed="true"])` negation (hover is an intermediate tint and must never demote an active badge), plus a `forced-colors` branch. Zero live regions, zero key handlers. **Deviations:** `value`/`onChange` map to `active`/bare `onToggle()` whose presence is itself the mode switch; `error`/`size`/`variant` N/A, `lang` N/A (`label` is consumer text — set `lang` on an ancestor) — documented; and, deliberately asymmetric to 3.4, there is **no** unwired-`active` dev warning, an omission carrying an explanatory comment in `status-badge-warnings.ts` and a test asserting the silence. **Structural escalation (Q10):** rest-vs-active is fill-colour-only because the check is drawn in every state, so forced colors flattens the visual distinction; the non-colour channel is the `role="img"` name (static) or `aria-pressed` (interactive), and since the design forbids a glyph-level distinction this is **logged and escalated to the designer, not invented around**, alongside the rest (1.20:1) and active (2.46:1) ratios routed to the accessibility-visuals PR per the Story 1.3 deferral.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `UiNotificationBadge` (`src/components/ui-notification-badge`)  | `new`  | No `crm`/`website` notification bell existed; the badge is composed 1:1 from the Figma Board A frames 294/5419–5420 (Rest `451:26194`, Hover `451:26199`, Active `451:26209`, Disabled `451:26214` — the design's four states), with interaction as one native action button carrying the same optional popup passthrough as `UiActionIconBar`, a plural-free Ukrainian name format (Ruling 5), and a **binding prohibition on live regions** — a count that changes while the user is elsewhere is the consumer's editorial decision, not the component's.                                                                                                                                                                           | Aligned 1:1 to the 48×48 masters: a `3rem` `'50%'` circle with a flex-centred `1.25rem` `bell-01` glyph and an absolutely-positioned `1.125rem` counter chip anchored right −4 / bottom 0, so the component paints 52×48 at rest, hover and disabled and **54×50 on active** — `overflow: hidden` is forbidden anywhere in the tree because it would clip the chip and its ring. Rest is the only state Figma gives a border, so per the no-jitter law the `1px` border is declared permanently over `border-box` and goes `transparent` in the other three. The active chip ring is an **outside** stroke (proven by the 54×50 vs 52×48 bounds) and therefore ships as `boxShadow: '0 0 0 2px backgroundGrey100'`, never a CSS `border`, which would eat into the 18px chip and shrink it. Chrome: the circle runs `backgroundGrey100` + `grey400` border → `alpha(primary, 0.1)` on hover → solid `primary` when active → `brandGray` when disabled, with the bell stepping `grey300` → `primary` → `white` → `grey400` and the chip `primary` → `grey400`; the ring colour is `#FBFBFB` (`backgroundGrey100`), **not** white — a "cut out of the grey page background" ring kept Figma-faithful, so the story and showcase surfaces sit on `#FBFBFB`. Counter type is Inter 500 12/18 white, centred on both axes. **"Active" semantics:** Figma's pressed column and an open panel are the same picture, so the active chrome keys off `&:active:not([aria-disabled="true"])` **and** `&[aria-expanded="true"]` — both attribute selectors on the root, no extra prop invented. **Name (Ruling 5), plural-free:** `count` is required and coerced to a non-negative integer (negative and non-finite become 0, fractions floor, both dev-warned), `label` defaults to `'Сповіщення'` and `max` to `9`; the display string is `String(count)` or `` `${max}+` ``, and the name is `` `${label}: ${display}` `` above zero and `label` alone at zero. "1 unread" would embed English pluralization, and Ukrainian plural forms make a baked plural word a localization defect. The name MUST use the **display** string — a capped chip is named "Сповіщення: 9+", never the raw count (SC 2.5.3) — the visible counter span is `aria-hidden` duplication, and at `count === 0` the chip is **not rendered at all**. **Live-region prohibition (binding, and the suite's headline assertion):** no `aria-live`, no `role="status"`, nothing announcing, in any state; the re-rendered `aria-label` is sufficient for on-focus/on-demand reading, and the JSDoc states the boundary so consumers neither double-wrap the badge in their own live region nor file bugs here. **Wired/static split on `onActivate`:** present → ONE native `<button type="button">` with the optional popup passthrough (`hasPopup` → `aria-haspopup="menu"`, `menuOpen` → `aria-expanded` in **both** states, `menuId` → `aria-controls` only while the menu is mounted — the 3.3 dangling-idref rule); absent → static content with no role, no `tabindex` and no ARIA. `disabled` uses the repo `aria-disabled` boundary (focusable, model-layer no-op, native `disabled` never set — SC 2.4.3) and is painted because Figma ships the column. The 48px target clears SC 2.5.8 with margin. Focus: because the rest/hover/active circle fills differ sharply, the ring is the **two-layer** `UiTaskCard` recipe (`inset 0 0 0 2px darkPrimary, inset 0 0 0 4px white`), exported so the unit test can pin it, inside the Amendment-A1 two-selector list and with a `forced-colors` branch. **Deviations:** `value` maps to the numeric `count` (the `UiPagination` numeric-value precedent) while `onChange` is N/A — the button announces intent, it never changes the count; `error`/`size`/`variant`/`lang` N/A (the default label is Ukrainian; override `label`) — documented; and no `getName` formatter ships in 3.5 (Q8 — consumers override `label`; adding it later must land the "output must contain the display string" dev-warning in the same change, so that contract's warning is N/A until then). The counter's white-on-`primary` 2.60:1 is **mitigated, not excused** (the span is `aria-hidden` duplication and the accessible name carries the same display string, so the count never depends on it) and joins the rest bell (2.70:1), rest border (1.45:1) and active bell (2.60:1) in the accessibility-visuals PR per the Story 1.3 deferral.                                                                                                                                                                                                                                                                                                                                                                                                          |

State/behaviour is locked by `tests/unit/ui-item-row.test.tsx` and
`tests/unit/ui-items-list.test.tsx` — render, the per-method and muted colour
recipes, the always-controlled APG disclosure button (`aria-expanded`,
`aria-controls` only while expanded, muted `onToggle` no-op), static vs wired
rendering, the content-derived accessible name, the dev-warning contract, and the
list's `<li>` wrapping + empty→renders-nothing behaviour — at 100% coverage.

`UiTaskCard` is locked by `tests/unit/ui-task-card.test.tsx` — wired/static
rendering, the exact content-derived accessible name, keyboard activation
(exactly-once, no key handlers), the `aria-disabled` boundary with focus
retention, the focus-return `ref`/`id` API, the informative-avatar contract and
its dev warnings, the no-clamp title, the live-region prohibition, and the pure
`taskCardSx` style assembly (focus ring, gated hover, chip no-jitter border) —
at 100% coverage.

`UiProfileSelectCard` is locked by `tests/unit/ui-profile-select-card.test.tsx`
— wired vs static rendering, the APG menu-button ARIA wiring (`aria-haspopup`,
the permanent `aria-expanded`, `aria-controls` only while the menu is mounted),
component-owned focus (open → first/last row by intent, wrap/Home/End, Escape
and activation → trigger, the close-underneath rescue that never drops focus to
`<body>`), the five close paths with the trigger-click double-fire exclusion,
the `aria-disabled` boundary and its dominance over `open`, the decorative
avatar (`alt=""`) and every §12 dev warning, the empty-items no-menu rule, the
live-region prohibition, and the exactly-once `onSelect` ordering — at 100%
coverage.

`UiIntegrationCard` is locked by `tests/unit/ui-integration-card.test.tsx` —
wired vs static rendering, the ARIA radio wiring (the permanent `aria-checked`
mirroring the always-controlled `selected`, no `aria-pressed`, exactly one
focusable element), exactly-once activation with the already-selected no-op and
declined-selection re-eligibility, the `aria-disabled` boundary with focus
retention, the decorative logo (`alt=""`) with its hygiene set and validation
guards, every §12 dev warning including the radiogroup-context matrix, the
live-region prohibition, and the pure `integrationCardSx` style assembly (the
constant 1px colour-only border, the gated hover, selected dominance, and the
Amendment-A1 two-selector focus ring) — at 100% coverage.

The six Story 3.5 Board A micro-components are locked by
`tests/unit/ui-filter-chip.test.tsx`, `tests/unit/ui-pin-input.test.tsx`,
`tests/unit/ui-payment-option-card.test.tsx`,
`tests/unit/ui-action-icon-bar.test.tsx`, `tests/unit/ui-status-badge.test.tsx`
and `tests/unit/ui-notification-badge.test.tsx` (+ the pin-input pure-logic
suites `tests/unit/pin-value-normalisation.test.ts`,
`tests/unit/pin-keyboard-intents.test.ts` and
`tests/unit/pin-paste-distribution.test.ts`, plus
`tests/unit/field-controls-glyph.test.tsx` covering the additive multi-path
`Glyph` branch the icon bar required) — wired vs static rendering with the
attribute-level zero-ARIA sweeps, exactly-once activation (including the pin
field's exhaustive typing/Backspace/Delete/arrow/paste matrix and its
`readOnly` + `aria-disabled` mechanism), the `aria-disabled` boundaries with
focus retention, every accessible-name rule (the chip's two-segment name plus
hidden suffix, the payment card's `alt={name}` channel, the icon bar's constant
toggle label, the status badge's two label regimes, the notification
display-string format at `0`/`1`/`9`/`10`/`99`), the popup-passthrough idref
rules, the live-region prohibition sweeps, every dev warning including the
radiogroup-context matrix and the deliberate unwired-`active` silence, and the
pure `sx` assemblies with their mutation-killing style pins (the raw shadow
literals, the constant colour-only borders, the checked border-width channel,
the seven verbatim icon paths and the Amendment-A1 two-selector lists) — at
100% coverage.

### Epic 4 — Skeleton Loading Experience Parity

**Shared baseline (Story 4.1, `#24`).** Every skeleton module below is reused from
`crm` — repository `git@github.com:VilnaCRM-Org/crm.git`, audited at commit
`0057d7845923b5f32fce7f276d384cdfcab5156c` — under `src/components/skeletons/…`.
The `website` repository (audited at `ca13841d91817c160ca42c27bd58af23b4c613f8`)
contains **no** skeleton components at all, so the canonical-source policy never
has to arbitrate: `crm` is the sole baseline and there is no visual gap-fill
source. PRD §3.2 (reuse-first delivery) and §3.4 (skeleton policy — animation
parity is **release-blocking**, redesign is forbidden) mandate the reuse. The
system is Emotion-`keyframes` based (neither repository ships a `MuiSkeleton`
theme override), which is what lets it cross the MUI 7 → 9 gap unchanged.
Reference IDs: issue [`#24`](https://github.com/VilnaCRM-Org/ui-toolkit/issues/24),
Figma Board D frame `538:38316` on canvas `439:19251`, and the CRM paths in the
table below.

**Verified animation parity** (line-level diff of `base/styles.ts` against
`ui-skeletons/base.ts`, locked by `tests/unit/skeleton-crm-parity.test.ts`):
identical shimmer keyframes (`background-position` `0% 0` → `100% 0`); identical
gradient `linear-gradient(90deg, rgba(211, 216, 224, 0) 0%, rgba(211, 216, 224, 0.6) 49.13%, rgba(211, 216, 224, 0) 100%)`
over `backgroundSize` `200% 100%`; identical `1.5s ease-in-out infinite alternate`
timing for **both** the shimmer and the shadow pulse; identical pulse shadows
`0px 7px 20px 0px rgba(211, 216, 224, 0.2)` → `0px 7px 60px 0px rgba(211, 216, 224, 0.8)`;
identical constants `SKELETON_BORDER_RADIUS '57px'`, `SKELETON_BORDER_COLOR '#E1E7EA'`,
`SMALL_MOBILE_BREAKPOINT 375` and `SMALL_MOBILE_BREAKPOINT_UPPER 376`.

**Deviations common to every row (non-animation, allowed).** Explicit TypeScript
annotations on the exported constants and style factories (the toolkit's stricter
typing rules); declaration ordering inside the modules; `@/components/skeletons/…`
alias imports rewritten to relative imports through the `ui-skeletons` barrel (the
`components-public-api` dependency-cruiser rule); the `UISkeleton*` identifiers
renamed to the toolkit's `UiSkeleton*` convention with `JSX.Element` returns
restated as `React.ReactElement`; and an added
`@media (prefers-reduced-motion: reduce) { animation: none }` guard (WCAG 2.2.2
Pause, Stop, Hide accessibility hardening, technique C39 — it only disables motion when the OS asks; the animated
path is byte-identical to CRM). **No animation behavior is changed and no visual
redesign is introduced.**

| Component                                                | Source | Rationale                                                                                                                                                                                                                                              | Alignment notes / deviations                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| -------------------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Skeleton animation base (`src/components/ui-skeletons`)  | `crm`  | CRM path `src/components/skeletons/base/styles.ts`. The single animation system every skeleton primitive routes through; PRD §3.4 makes its timing/easing/keyframes release-blocking, so it is reused verbatim rather than re-derived from the design. | Carries `shimmerAnimation`, `shadowPulseAnimation`, `shimmerGradient`, `baseSkeletonStyle` and the four constants with the CRM values listed above. **Deviations:** the shared TypeScript/ordering/import set, plus the `prefers-reduced-motion` guard, which lives here (not in each primitive) precisely because every primitive spreads `baseSkeletonStyle` — one guard stops the shimmer everywhere. A `./base` → `index.ts` barrel was added so consumers never reach into the implementation file (`components-public-api` boundary rule); CRM has no such barrel. Any drift now fails `tests/unit/skeleton-crm-parity.test.ts`.                                                                                                                                                                                                                            |
| `UiSkeletonText` (`src/components/ui-skeleton-text`)     | `crm`  | CRM path `src/components/skeletons/ui-skeleton-text/`. The one-line/many-line text placeholder that Epic 4's primitive variants build on; reused so text placeholders shimmer identically across products.                                             | Size→height map, `SKELETON_BORDER_RADIUS` corner and `width` passthrough are the CRM values. **Deviation:** CRM's `TextSkeletonStyles` class with a `build(size, width)` method is restated as the plain `getTextSkeletonStyles(size, width)` factory (the toolkit's no-static/classes-only rule bans a class used purely as a namespace), and CRM's `StylesObject` alias is replaced by an inline structural return type. A Storybook story (`skeleton-text.stories.tsx`) was added; CRM ships none for this primitive.                                                                                                                                                                                                                                                                                                                                          |
| `UiSkeletonButton` (`src/components/ui-skeleton-button`) | `crm`  | CRM path `src/components/skeletons/ui-skeleton-button/`. The button-shaped placeholder; reused so button loading states keep CRM's border colour and small-mobile sizing.                                                                              | `buttonSkeleton` geometry, `SKELETON_BORDER_COLOR` border, `SKELETON_BORDER_RADIUS` corner and the `SMALL_MOBILE_BREAKPOINT` media query are the CRM values. **Deviation:** the `sx` normalisation is typed explicitly (`Extract<SxProps<Theme>, readonly unknown[]>[number][]`) where CRM relies on inference, plus the shared TypeScript/import set. A Storybook story (`skeleton-button.stories.tsx`) was added.                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `UiSkeletonInput` (`src/components/ui-skeleton-input`)   | `crm`  | CRM path `src/components/skeletons/ui-skeleton-input/`. The field placeholder, including the `::after` inset trick that fakes a 1px border out of the shimmer itself; reused so form loading states match CRM exactly.                                 | `BASE_INPUT_HEIGHT 3`, `MD_INPUT_HEIGHT 4.9375`, `XL_INPUT_HEIGHT 4`, the `clamp()` height, the responsive `minWidth` ladder, the `staticSkeleton` opt-out and the `inputPlaceholder` geometry are the CRM values. **Deviation:** CRM's `SKELETON_INPUT_OUTER_BACKGROUND` and `SKELETON_INPUT_BORDER_GRADIENT` exports are **not** ported — they are dead exports in CRM (declared, referenced nowhere in its `src` or `tests`), and carrying them would put a second, divergent gradient literal in the toolkit's public surface. A Storybook story (`skeleton-input.stories.tsx`) was added.                                                                                                                                                                                                                                                                    |
| `UiSkeletonBlock` (`src/components/ui-skeleton-block`)   | `crm`  | CRM path `src/components/skeletons/ui-skeleton-block/`. The generic rectangular placeholder Epic 4's composed layouts assemble from; reused so block fills shimmer identically.                                                                        | Width/height/radius passthrough over the spread `baseSkeletonStyle` is the CRM behavior. **Deviation:** the shared TypeScript/ordering/import set only; a Storybook story (`skeleton-block.stories.tsx`) was added.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `AuthSkeleton` (`src/components/auth-skeleton`)          | `crm`  | CRM path `src/components/skeletons/auth-skeleton/`. The composed auth-form loading layout and the only consumer of `shadowPulseAnimation`; reused so the sign-in/sign-up loading experience is behaviorally identical across products.                 | `formWrapperPulse` runs `shadowPulseAnimation` at the CRM `1.5s ease-in-out infinite alternate`; the title/subtitle/field/button geometry and the `SMALL_MOBILE_BREAKPOINT`/`_UPPER` and `AUTH_SKELETON_TINY_BREAKPOINT '336px'` media queries are the CRM values. **Deviations:** CRM's `base/auth-form-shared-styles.ts` is co-located as `auth-skeleton/shared-styles.ts` (it is consumed only here, so `ui-skeletons` stays a pure animation barrel); the pulse gets its own `prefers-reduced-motion` guard, since it is declared outside `baseSkeletonStyle`; and CRM's nested `'&:last-child': { marginBottom: 0 }` inside the `SMALL_MOBILE_BREAKPOINT` block is dropped as dead CSS — that same block already sets `marginBottom: 0` on the base selector, so the nested override resolves to the identical value and the rendered geometry is unchanged. |

Animation parity is locked by `tests/unit/skeleton-crm-parity.test.ts` — the exact
shimmer gradient string, `backgroundSize`, the shimmer-not-pulse keyframes
identity, the `1.5s ease-in-out infinite alternate` timing, the reduced-motion
guard, both keyframe stop sets and all four constants — so any future drift from
the CRM contract fails CI rather than shipping. Render and responsive behavior for
the primitives and the composed auth layout stay locked by
`tests/unit/ui-skeletons.test.tsx` and `tests/unit/auth-skeleton.test.tsx`. Story
4.1 changes no runtime code: it identifies the baseline, verifies it, and locks it.

**Accessibility hardening (2026-08-04, per accessibility-lead review).** The
skeleton suite was retrofitted to the canonical conventions: primitive roots
(`UiSkeletonText`/`Block`/`Button`/`Input`/`Image`) carry `aria-hidden="true"`
and nothing else; composed layouts (`AuthSkeleton`) render the `ComposedSkeleton`
shape — a plain `aria-busy` container, visually-hidden status text, and an
`aria-hidden` shape tree — instead of a named `<section>` landmark, with
`React.useId()`-scoped ids; and forced-colors fallbacks were added
(`outline: 1px solid GrayText` on `baseSkeletonStyle`, `border: 1px solid
CanvasText` on `formWrapperPulse`) because Contrast Themes strip the gradient and
shadows. The reduced-motion citation is corrected to WCAG 2.2.2 Pause, Stop, Hide
(pre-load/essential exception) with technique C39. The animation contract is
unchanged and `tests/unit/skeleton-crm-parity.test.ts` still passes untouched.

Stories 4.2 and 4.3 (#25/#26) complete the Board D skeleton scope on top of that
locked baseline. Every module below reuses the CRM animation system unchanged
(`baseSkeletonStyle` / the shared primitives) and takes its geometry from the
Board D frame `538:38316` ("Skeletons") on canvas `439:19251`, measured live via
the Figma MCP; the shared composed shell applies the accessibility conventions
recorded above.

| Component                                                             | Source | Rationale                                                                                                                                                                                                         | Alignment notes / deviations                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| --------------------------------------------------------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `UiSkeletonImage` (`src/components/ui-skeleton-image`)                | `new`  | Board D requires round and block image placeholders (#25); no CRM/website source exists, so the visuals come from nodes `538:38680`/`538:38681` (48×48 round) and `538:39442` → `538:39440` (260×195, 8px block). | Both variants spread `baseSkeletonStyle`; the measured node fill is exactly `shimmerGradient`, so no new colour or animation entered the system. Variant-derived dimension defaults (48×48 / 260×195) instead of `UiSkeletonBlock`-style literals — switching `variant` switches both dimensions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `UiSkeletonText` many-lines extension                                 | `new`  | Board D "Text many lines" (#25): nodes `538:38695`–`538:38697` (197/157/96 × 8, 14px pitch).                                                                                                                      | Additive `lines` prop on the CRM-sourced primitive; `lines === 1` keeps the CRM markup byte-identical. Taper `100% → 80% → 50%`, 6px gaps, 8px rows (`size` resolves `s` for multi-line unless set). Recorded here because the CRM row above documents the baseline component this extension rides on.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `ComposedSkeleton` shell (`src/components/ui-skeletons/composed.tsx`) | `new`  | Shared internal shell for every composed layout (#26): nameless `aria-busy` container + visually-hidden status text + `aria-hidden` shape tree, per the accessibility-lead ruling.                                | Internal (not on the public export surface). Default status text `Завантаження`, overridable per instance for localization; announcements stay the consumer's job (documented in JSDoc).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `UiSkeletonWidget` (`src/components/ui-skeleton-widget`)              | `new`  | Board D widget cards (#26): small `538:38698`/`538:38709`/`538:38721`/`538:39423`/`538:39430`/`632:46278`, medium `632:46444`–`632:46506`.                                                                        | `size: small\|medium` × `variant: task-list\|block\|chart`; `columns: 1\|2` doubles as the medium task-list footprint switch (774×410 vs 1167×540 two-column); it applies to `variant="task-list"` only — every other variant resolves to one column, so the wide footprint is unreachable for block and chart. Chart bars carry the measured 7-bar silhouette of the small (774) chart. **Story 5.1 deviations (documented, not delivered):** Board D also paints wide 1167×540 block widgets (`632:46474` in row `632:46446`; `632:46507`, `632:46513` in row `632:46506`) and a wide chart widget (`632:46519`, seven ~150px-pitch bars at 46/61/53/146/210/104/141) that the component cannot render (checklist D-17); and the wide task-list card `632:46480` paints a distinct dual-avatar anatomy — five 86px rows per column on a 94px pitch, 34×34 avatars at both x16 and x509, one 269×12 title bar plus one ~145×8 meta bar — which the shared single-avatar 94px anatomy from `632:46447` stands in for, so "matching the board" holds for the task-list footprint switch but not for that card (checklist D-18). |
| `UiSkeletonMenu` (`src/components/ui-skeleton-menu`)                  | `new`  | Board D menu sidebar `538:39489` (238×791) (#26).                                                                                                                                                                 | Six 54px nav rows, expanded section (24px icon dot, title bar, three 115×14 sub-rows), divider, trailing row — geometry as measured.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `UiSkeletonTabBar` (`src/components/ui-skeleton-tab-bar`)             | `new`  | Board D tab bar `538:39646` (1132×39) (#26).                                                                                                                                                                      | Six 147×18 label bars on the 189px pitch over the 4px track with per-tab underline segments; `tabs` overrides the count. Decorative divs only — no `tab`/`tablist` roles.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `UiSkeletonList` (`src/components/ui-skeleton-list`)                  | `new`  | Board D list rows `538:39708`/`538:39713`/`538:39719` (#26).                                                                                                                                                      | The three board nodes measure as byte-identical 590×64, 8px-radius leaf bars (no avatar/text anatomy — verified via node metadata and design context), so the component repeats one row anatomy `rows` times at the measured 6px gap; fluid `width: 100%` capped at the 590px board width.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `UiSkeletonControlText` (`src/components/ui-skeleton-control-text`)   | `new`  | Board D "Ckeckbox + Text" `538:39802` / "Radiobutton + Text" `538:39808` (#26).                                                                                                                                   | 24×24 control (8px radius checkbox / circle radio) + 147×18 bar, 8px gap. **Deviation:** the board paints the control a flat `#EBEEF2` (the static form of the shimmer's peak tone over white); the component uses the shimmer via `UiSkeletonBlock` instead, keeping the no-new-colours rule and in-step animation. No `checkbox`/`radio` roles — plain divs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `UiSkeletonTable` (`src/components/ui-skeleton-table`)                | `new`  | Board D table `538:40309` (1166×692) (#26).                                                                                                                                                                       | Header is five loose 63×14 bars over ten 1166×56 rows (12px gap); column tracks 228/196/117/219/324 close to the pixel against the 1166 frame. Plain divs throughout — no table elements or `table`/`grid`/`row`/`cell` roles (accessibility ruling); width cycles when `columns` exceeds the design set; fluid `width: 100%` capped at 1166px.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |

Board D's `Button` (`538:39683`, 166×62) and `Input` (`538:39789`, 513×91)
patterns are covered by the CRM-baseline `UiSkeletonButton` / `UiSkeletonInput`
rather than forked (#26, measured verification): the button matches radius,
`#E1E7EA` stroke, fill and (at the xl breakpoint) the 62px content height, with
the fluid CRM width and a 2px outer-height difference from content-box sizing
against Figma's inside stroke as the only deltas; the input field matches radius,
inner-bar geometry and xl height, with the board's 147×18 label bar left to the
caller as a `UiSkeletonText size="l"` composition and CRM's animated shimmer ring
standing in for the board's static 1px stroke of the same grey family.
