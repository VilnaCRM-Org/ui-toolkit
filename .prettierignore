# Ignore artifacts:
build
coverage
.next
.lighthouseci
out
storybook-static
.github
.playwright-mcp

# Generated by TriPSs/conventional-changelog-action on every release, in its own
# formatting. Prettier would reformat it and the next release would undo that, so
# a release commit landing on main would leave `make format-check` permanently
# red. .markdownlintignore already exempts it for the same reason.
CHANGELOG.md
